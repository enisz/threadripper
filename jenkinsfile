pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                echo 'Hello World'

                script {
                    echo sh(returnStdout: true, script: 'pwd')
                    echo sh(returnStdout: true, script: 'ls -lrth')
                }
            }
        }
    }
}
